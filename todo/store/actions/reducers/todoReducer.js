import * as actionTypes from '../actionTypes';

const intialState={
    input: 'asd'
}
// todo: ['blah', 'hehe']

const reducer = (state=intialState, action)=>{
    switch (action.type) {
        // case value:
            
        //     break;
    
        default:
            return state;
    }
}

export default reducer;